apiVersion: v1
kind: Service
metadata:
  name: spark-fraud-detection-service
spec:
  type: NodePort
  selector:
    app: spark-fraud-detection
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 30088 # The notebook will be accessible on this port on your local machine
